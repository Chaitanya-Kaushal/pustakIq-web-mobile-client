{
  "name": "pustakiq",
  "version": "0.1.0",
  "private": true,
  "description": "PustakIQ — India's hyperlocal education community platform (monorepo)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start -w @pustakiq/mobile",
    "mobile:ios": "npm run ios -w @pustakiq/mobile",
    "mobile:android": "npm run android -w @pustakiq/mobile",
    "web": "npm run dev -w @pustakiq/web",
    "web:build": "npm run build -w @pustakiq/web",
    "web:start": "npm run start -w @pustakiq/web",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "4.3.1",
    "tailwindcss": "4.3.1",
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  },
  "comment": "tailwindcss is pinned to v4 at the root so the Next.js web app (Tailwind v4) resolves it correctly; the mobile app keeps its own nested Tailwind v3 (NativeWind) under apps/mobile.",
  "engines": {
    "node": ">=20"
  }
}
